spring:
  application:
#    微服务命名不能有下划线！！！  否则restTemplate调用识别不到
    name: provider-nacos-discovery
  profiles:
    active: dev
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher

server:
  port: 9090